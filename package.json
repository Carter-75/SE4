{
  "name": "appointment-booker",
  "version": "1.0.0",
  "scripts": {
    "dev": "node scripts/dev-launcher.js",
    "build": "cd frontend && npm install && npm run build",
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "start": "node start-dev.js",
    "sync": "py sync-env.py || python3 sync-env.py || python sync-env.py",
    "push-sync": "npm run sync && git push"
  },
  "dependencies": {
    "express": "^4.19.2",
    "mongoose": "^8.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "helmet": "^7.1.0",
    "morgan": "~1.10.0",
    "cookie-parser": "~1.4.6",
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.0"
  }
}